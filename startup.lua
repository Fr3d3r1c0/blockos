function say(string, times)
  for i=1, times do
    print(string)
  end
end

-- variables
  -- OS info
    osName = "BlockOS"
    osVersion = "1.0.0"
    osOwner = "Project09"
  -- deffault settings
    -- user config
      userName = {"", "", "", ""}
      userTheme = {"dark", "dark", "dark", "dark"}
